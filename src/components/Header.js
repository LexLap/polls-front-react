import React from 'react';

const Header = (props) => {
    
    return(
        <div id="header-container">

            <div>
                <h1>Poll Application</h1>
            </div>

        </div>
    );
};

export default Header;